version: "3.9"
services:
  nourishai:
    build: .
    ports:
      - "8501:8501"
    env_file: .env
    volumes:
      - .:/app
    command: streamlit run ui/app.py --server.port 8501 --server.headless true --server.address 0.0.0.0